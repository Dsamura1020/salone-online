// Main schema: generator + datasource only.
// Models live in prisma/models/*.prisma; enums in prisma/enums.prisma

generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["postgresqlExtensions"]
}

datasource db {
  provider   = "postgresql"
  extensions = [vector, pgcrypto]
}
